[project]
name = "cloudflare-mcp"
version = "0.1.0"
description = "MCP server for the Cloudflare API: zones and DNS record management."
authors = [
    { name = "Jan Ivan Simoy", email = "jimsimoy@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "mcp>=2.0.0",
    "python-dotenv>=1.0",
]

[project.scripts]
cloudflare-mcp = "cloudflare_mcp:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
